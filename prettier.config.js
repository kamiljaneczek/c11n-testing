/** @type {import('prettier').Config & import('prettier-plugin-tailwindcss').PluginOptions} */
const config = {
  plugins: [],
  singleQuote: false,
  semi: true,
  printWidth: 120,
  trailingComma: "es5",
  tabWidth: 2,
  useTabs: false,
};

export default config;